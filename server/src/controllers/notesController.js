const { validateNote, formatNote } = require('../noteUtils');
const store = require('../store/notesStore');

function createNote(req, res) {
  // not implemented yet
}

function getAllNotes(req, res) {
  // not implemented yet
}

module.exports = { createNote, getAllNotes };